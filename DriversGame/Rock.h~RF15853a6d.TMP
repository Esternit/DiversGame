#pragma once
#include <SFML/Graphics.hpp>

class Rock {
private:
	sf::Sprite rock;
	int imagePos = 1;
public:
	Rock(const sf::Texture& textures, float x, float y);

	void updateTexture(const sf::Texture& textures);

	void setPosition(float x, float y);

	sf::Sprite getSprite() const;
};