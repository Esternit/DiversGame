#include "Rock.h"

using namespace sf;

Rock::Rock(const sf::Texture& textures, float posX, float posY) {
	rock.setTexture(textures);
	rock.setPosition(posX, posY);
	rock.setScale(2.f, 2.f);
}

void Rock::updateTexture(const sf::Texture& textures) {
	rock.setTexture(textures);
}

Sprite Rock::getSprite() {
	return rock;
}